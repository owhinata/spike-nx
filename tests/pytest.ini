[pytest]
addopts = -sv
markers =
    slow: marks tests as slow (ostest, coremark) - deselect with '-m "not slow"'
    interactive: marks tests requiring manual operation - always run last
    no_memcheck: disable check_memory_leak fixture for this test
