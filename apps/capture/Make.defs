############################################################################
# apps/capture/Make.defs
############################################################################

ifneq ($(CONFIG_APP_CAPTURE),)
CONFIGURED_APPS += $(APPDIR)/external/capture
endif
